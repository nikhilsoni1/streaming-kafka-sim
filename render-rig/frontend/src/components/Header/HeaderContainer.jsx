// components/HeaderContainer.jsx
import Header from '../Header';

export default function HeaderContainer() {
  // No logic needed here for now, but structure is ready for future state/side-effects
  return <Header />;
}
